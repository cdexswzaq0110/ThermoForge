# ThermoForge 共享語言

> 這份檔是本專案的**共享記憶體區段**。人與 Agent 讀同一份定義。
> 維護方式見 `.claude/skills/se-context-language`。
> 現在只填「已經造成過歧義」與「一定會被叫錯」的詞，其餘等它們長出來。

## Language

| 詞 | 一句話定義 | 避免的舊叫法 |
|---|---|---|
| **佈局**（layout） | 一個板級熱設計案例的**完整輸入**：板子幾何、每個元件的位置／尺寸／功耗、材料導熱係數、對流係數 h、環境溫度 | 「case」「樣本」——樣本是佈局經求解器算完之後的（輸入, 輸出）配對 |
| **功率圖**（power map） | 佈局離散化到網格之後，每個網格格點的體積功率密度場，單位 W/m³；代理模型的實際輸入張量 | 「功率」——那是元件層級的純量，不是場 |
| **溫度場**（temperature field） | 求解器或代理模型輸出的 H×W 穩態溫度陣列，單位 °C | 「溫度」——沒有指名是場還是熱點溫度的說法一律視為未定義 |
| **熱點溫度**（T_max） | 一個溫度場中的最大值。決策真正看的那個數字 | 「最高溫」「峰值」 |
| **求解器**（solver） | 本 repo 內建的二維穩態熱傳導有限差分求解器。**本專案唯一的 ground truth 來源** | 「CFD」——它不是 CFD，混用會直接造成越界宣稱 |
| **代理模型**（surrogate） | 學習「功率圖 → 溫度場」這個映射的神經網路。它逼近的是求解器，不是物理現實 | 「模型」——單講模型分不出是求解器還是代理模型 |
| **物理基準**（physics baseline） | 用 Green's function 疊加做的解析近似解。代理模型的**品質底線**，不是陪襯 | 「baseline」——單講 baseline 會被誤解成預測平均場的 dummy |
| **熱點餘裕**（thermal margin） | `T_allowed − T_max_pred`。正值代表還有餘裕，負值代表超規 | 「安全係數」——那是另一個有既定工程定義的詞 |
| **base layout** | 板子尺寸 ＋ 元件位置與大小 ＋ 功率**比例**。切分時的**組別**（group） | 「樣本」——一個 base layout 配上一組操作條件才是一個樣本 |
| **候選池**（screening pool） | 一次真實設計迭代的模擬：池內板子、元件尺寸、功率比例、總功率、kt、h、T_amb **全部固定**，只有元件位置在變 | 「驗證集」——池是評估**場景**，不是切分。主要 metric 只能在池內算 |
| **無因次化** | 把功率圖除以 P/A、溫升除以 θ_ref = P/(h·A)，長度以 1/m 為單位。除完之後方程只剩 (p_norm, m·Lx, m·Ly) → θ_norm | 「正規化」——那是從資料估統計量；無因次化的兩個尺度是**精確算出來**的 |
| **物理基準場**（greens_norm） | 無因次座標系裡的 Green's 疊加解。代理模型的一個輸入通道，也是 residual 變體的目標基準 | 「先驗」「初始猜測」——它是一個確定的解析函數，不含任何學到的參數 |
| **id-val** | 與訓練同分佈的驗證切分（5 折 OOF），用來調參，可以反覆看 | 「validation set」——不指名 id 還是 ood 的說法視為未定義 |
| **ood-holdout** | 功率、元件數、對流係數或板型長寬比**超出訓練範圍**的凍結切分。整個專案只解凍一次 | 「測試集」——本專案沒有可反覆評估的測試集 |
| **設計查詢**（design query） | LLM 層把自然語言解析出來的結構化物件：改動哪個元件、改成多少、要滿足什麼約束 | 「prompt」——prompt 是使用者原文，design query 是解析後的結構 |

## Relationships

- 一個 **佈局** 經 **求解器** 算一次，產生一個 **樣本**（佈局, 溫度場）。
- 一個 **base layout** 會配上多組操作條件（kt、h、T_amb、目標熱點溫升）衍生出多個樣本
  → 它們高度相關，是同一組，切分時必須整組同進同出。
- 一個 **候選池** 也共用一個幾何範本，但變的是**位置**而不是條件——
  這是它與 base layout 的分工：base layout 固定位置變條件，池固定條件變位置。
- **代理模型** 與 **物理基準** 吃同一個 **功率圖**，輸出同一種 **溫度場**，因此可以直接比。
- **設計查詢** 觸發一次或多次 **代理模型** 呼叫；**熱點餘裕** 是回覆給使用者的決策量。

## Flagged ambiguities

- 「誤差」曾同時指**整場 MAE** 與**熱點溫度誤差** —— 已解決：一律寫全名。
  兩者不可互相代表：整場 MAE 很低但熱點低估 8°C 的模型是不可用的模型。
- 「準確」在熱設計裡預設是**不對稱**的 —— 已解決：低估（predict < truth）比高估貴，
  任何只報對稱指標（MAE／RMSE）的結論都必須同時報低估率。

## Out of scope

- **暫態**（transient）、**流場**（flow field）、**輻射**（radiation）：求解器不解，
  所以本專案不定義這些詞，也不得在結論裡出現。
- **CFD 相對誤差**：沒有 CFD 參考解，這個量在本 repo 內無法計算，不要造一個出來。
