# P<NN> — <Process 標題>

一個 **Process** = 可獨立派發、獨立驗證的執行單元。分解判準見 [`.claude/EXECUTION_MODEL.md`](../.claude/EXECUTION_MODEL.md)。

---

- **來源**：`<計畫或規格路徑>` 的 <段落>
- **Depends on**：無 ／ `P<NN>`（**只有真的需要它的產物或決策時才填**）
- **Write lock**：無 ／ `<實際會寫入的檔案、模組、Schema、共用資源>`
- **建議派發單位**：Process ／ Thread ／ Coroutine（<一句理由>）

> `Depends on` 與 `Write lock` 是兩件事。
> 依賴 = 順序固定；鎖 = 不能同時跑但順序自由。
> **把寫入衝突標成依賴會憑空製造一條假的關鍵路徑。**

## 交付成果

從**使用者或系統行為**描述這個 Process 完成後實際成立的結果。

不是「實作 X 模組」，是「使用者做了 A 之後會看到 B」。

## 驗收條件

- <可觀察、可操作、可重現的判定方式>
- <同上>

> 「完成實作」不是驗收條件——那只是在宣稱做完。
> 「測試通過」可以當**證據**，但不能取代功能本身應達成的結果。
> 有介面的，驗收條件要包含實際操作流程。

## 測試接縫

<測試放在哪個已確認的接縫上。沒確認過的接縫不是有效的測試位置。>

## 必跑指令

```bash
<實際要跑的驗證指令>
```

## 內部 Thread（可省略）

這個 Process 內部可以平行的唯讀子工作。**寫入範圍相交的不要列在這裡。**

| Thread | 做什麼 | 只讀什麼 |
|---|---|---|

---

## 執行紀錄（由 Scheduler 填寫）

| 欄位 | 值 |
|---|---|
| 派發單位 | Process ／ Thread ／ Coroutine |
| 角色 | Worker ／ Supervisor |
| 實際 Agent／CLI | |
| 實際模型 | |
| 實際推理強度 | |
| 持有的鎖 | |
| 難度判定與理由 | |
| 基準 SHA | |
| Review revision | |
| 狀態 | PENDING／DISPATCHED／PASS／FIX／ESCALATED／BLOCKED |
| 重試次數 | |

> 取不到實際模型或推理強度就如實填 `unknown`。**不得把預期值當成執行證據。**
